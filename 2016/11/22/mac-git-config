1. 下载git:官网http://git-scm.com/download/
2. 安装git后，在命令行输入git --version 测试是否安装成功
3. 配置git本地信息：用户名和邮箱账号：
  （1）git config --global user.name "your name"
  （2）git config --global user.email "your email"
  
4. 生成sshkey


创建分支：git branch <branch-name>
基于分支branchBase创建分支branchA : git branch branchA branchBase
创建的本地分支，推到远程仓库 : git push origin <branch-name>
新创建的本地分支与对应的远程分支同步信息（首次）: git branch --set-upstream-to=origin/<branch-name> <branch-name>
